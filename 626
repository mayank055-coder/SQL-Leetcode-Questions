https://leetcode.com/problems/exchange-seats/description/
# Write your MySQL query statement below
select 
    id,
    case when id%2 = 1 then lead(student,1,student) over() else lag(student) over() end as student
from Seat
